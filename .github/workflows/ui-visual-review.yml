name: UI Visual Review

on:
  push:
    branches: [main]
    paths:
      - 'games/scrap-factory/adaptive-ui.js'
      - 'games/scrap-factory/adaptive-ui.css'
      - 'games/scrap-factory/progression-ui.js'
      - 'scripts/ui-visual-review.mjs'
      - '.github/workflows/ui-visual-review.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  chromium-review:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install browser review dependencies
        run: npm install --no-save --package-lock=false three@0.185.0 puppeteer-core@24.16.0
      - name: Use local Three.js in CI checkout
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('games/scrap-factory/index.html')
          text = path.read_text()
          text = text.replace('https://cdn.jsdelivr.net/npm/three@0.185.0/build/three.module.js', '/node_modules/three/build/three.module.js')
          path.write_text(text)
          PY
      - name: Start local site
        run: |
          python3 -m http.server 4173 > /tmp/game-ui-server.log 2>&1 &
          echo $! > /tmp/game-ui-server.pid
          sleep 1
      - name: Review adaptive HUD in Chromium
        run: node scripts/ui-visual-review.mjs
      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adaptive-ui-review-${{ github.run_number }}
          path: artifacts/ui-review/
          if-no-files-found: warn
